import React from 'react';
import {Container} from '@material-ui/core';

export const Footer = () => {
  return (
	<footer className='footer' style={{background: '#121e62', minHeight: '50px', color: '#ffffff', display: 'flex', marginTop: 'auto'}}>
		<Container>
		Hi
		</Container>
	</footer>
  );
};
