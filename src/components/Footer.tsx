import React from 'react';
import {Container} from '@material-ui/core';

export const Footer = () => {
  return (
	<footer style={{background: '#121e62', minHeight: '50px', color: '#ffffff'}}>
		<Container>
		Hi
		</Container>
	</footer>
  );
};
